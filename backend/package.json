{
  "name": "designcritic-ai-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Secure backend that sends uploaded designs to a Gemini vision model and returns structured, grounded design critiques.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "helmet": "^8.0.0",
    "multer": "^2.1.1",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
